spring:
  datasource:
    url: jdbc:mysql://${com.ssafy.db.address_and_port}/b106?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Seoul&zeroDateTimeBehavior=convertToNull&rewriteBatchedStatements=true
    username: ${com.ssafy.db.username}
    password: ${com.ssafy.db.password}

jwt:
  secret: ${com.ssafy.jwt.secret}

com:
  ssafy:
    kakao:
      client_id: ${com.ssafy.kakao.client_id}
      redirect_uri: ${com.ssafy.kakao.redirect_uri}
